# Public site URL used to construct short links
SITE_URL=https://sharethis.site

# Firebase Admin SDK (server-side only). Do NOT prefix with PUBLIC_
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project-id.iam.gserviceaccount.com
# Keep \n escapes; code will convert them to real newlines at runtime
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nYOUR_KEY\\n-----END PRIVATE KEY-----\\n"
